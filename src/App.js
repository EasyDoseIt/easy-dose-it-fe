import React from 'react';
import './App.css';

function App() {

  // const [isLoading, setIsLoading] = useState(true);

  // useEffect(() => {
  //   setTimeout(() => setIsLoading(false), 6000);
  //   console.log('timeout')
  // }, []);

  return (
    <>
    {/* {isLoading === false ? (
        <div className="App">
          <TopAppBar />
        </div>
      ) : (
        <div className="App">
          <SplashScreen />
        </div>
          )} */}
    </>
  );
}

export default App;
